import React,{useEffect} from 'react';import {NavigationContainer,DarkTheme as NavDark,DefaultTheme as NavLight} from '@react-navigation/native';import {createNativeStackNavigator} from '@react-navigation/native-stack';import {createBottomTabNavigator} from '@react-navigation/bottom-tabs';import {Ionicons} from '@expo/vector-icons';import {useTheme} from '../theme';
import LoginScreen from '../screens/auth/LoginScreen';import RegisterScreen from '../screens/auth/RegisterScreen';import HomeScreen from '../screens/HomeScreen';import CallsScreen from '../screens/CallsScreen';import MessagesScreen from '../screens/MessagesScreen';import ContactsScreen from '../screens/ContactsScreen';import SettingsScreen from '../screens/SettingsScreen';import ChatScreen from '../screens/ChatScreen';import DialerScreen from '../screens/DialerScreen';import CallScreen from '../screens/CallScreen';import {useAuth} from '../store/AuthContext';
import {getSocket} from '../services/socket';
const Stack=createNativeStackNavigator();const Tabs=createBottomTabNavigator();
export default function AppNavigator(){const {user,loading}=useAuth();const t=useTheme();const navRef=React.useRef<any>(null);useEffect(()=>{if(!user)return;const s=getSocket();const incoming=(d:any)=>{if(navRef.current?.isReady())navRef.current.navigate('Call',{callId:d.callId,mode:'incoming',peer:d.caller,offer:d.offer});};s.on('call:incoming',incoming);return()=>{s.off('call:incoming',incoming)}},[user]);if(loading)return <HomeScreen/>;return <NavigationContainer ref={navRef} theme={t.dark?NavDark:NavLight}>{user?<Stack.Navigator screenOptions={{headerShown:false}}><Stack.Screen name="Main" component={TabsNav}/><Stack.Screen name="Chat" component={ChatScreen}/><Stack.Screen name="Dialer" component={DialerScreen}/><Stack.Screen name="Call" component={CallScreen}/></Stack.Navigator>:<Stack.Navigator screenOptions={{headerShown:false}}><Stack.Screen name="Login" component={LoginScreen}/><Stack.Screen name="Register" component={RegisterScreen}/></Stack.Navigator>}</NavigationContainer>}
function TabsNav(){const t=useTheme();return <Tabs.Navigator screenOptions={({route})=>({headerShown:false,tabBarActiveTintColor:t.accent,tabBarInactiveTintColor:t.muted,tabBarStyle:{backgroundColor:t.card,borderTopColor:t.line,height:64,paddingTop:6},tabBarIcon:({color,size})=><Ionicons name={route.name==='Home'?'home':route.name==='Calls'?'call':route.name==='Messages'?'chatbubbles':route.name==='Contacts'?'people':'settings'} color={color} size={size}/>})}><Tabs.Screen name="Home" component={HomeScreen} options={{title:'Home'}}/><Tabs.Screen name="Calls" component={CallsScreen}/><Tabs.Screen name="Messages" component={MessagesScreen}/><Tabs.Screen name="Contacts" component={ContactsScreen}/><Tabs.Screen name="Settings" component={SettingsScreen}/></Tabs.Navigator>}
