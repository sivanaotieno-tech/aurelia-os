# E.V. local backend

FastAPI service for local AI, speech, memory and permission-gated Windows tools.

Run from the repository root with:

```powershell
npm run backend:run
```

The service listens on `127.0.0.1:8765` by default.
